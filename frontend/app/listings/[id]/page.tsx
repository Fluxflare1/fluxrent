// inside listing detail (client component)
import BoostButton from "@/components/listings/BoostButton";

<BoostButton listingId={listing.id} onBoosted={() => { /* refetch listing or update UI */ }} />
