{
  "name": "property-tenant-mgmt-mvp",
  "private": true,
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "dev": "concurrently \"npm --workspace frontend run dev\" \"docker compose up backend\"",
    "build": "npm --workspace frontend run build",
    "start": "concurrently \"npm --workspace frontend run start\" \"docker compose up backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
